import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'support_app_2',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
